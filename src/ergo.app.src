{application, ergo, [
	{description, "A parallel build runner."},
	{vsn, "0.1.0"},
	{modules, []},
	{registered, []},
	{applications, [
		kernel,
		stdlib,
                crypto
                %removed mnesia - ergo requires that it be loaded and not running
	]},
	{mod, {ergo_app, []}},
	{env, [
              ]}
]}.
